const MODEL='gpt-image-2.5-sunburst';
const MAX_SOURCE_BYTES=2_500_000;
const PAID_GENERATION_ENABLED=process.env.READY_CHARACTER_PAID_GENERATION==='true';
const IDENTITY_MASTER=`Use the uploaded child photo as the sole identity authority. Preserve the SAME CHILD with very high fidelity. Precisely preserve face shape and width-to-height ratio, forehead, eye shape/size/spacing, eyebrow shape and position, nose bridge/tip/width, mouth shape and smile geometry, cheeks, jaw/chin, ears when visible, hairline, hairstyle and the child's overall recognizable impression. Do not beautify, idealize, age up/down, change ethnicity, enlarge eyes excessively, shrink the nose, narrow the jaw, or replace the face with a generic cute child. Temporary face paint, stickers, lighting and camera artifacts are NOT identity features. Natural age-appropriate proportions only.`;
const QUALITY_MASTER=`Render as a warm, bright, premium high-density 2.5D editorial explorer character for Ready & Set. Polished animated-character key-visual quality, dimensional but not photorealistic: refined soft skin shading, dimensional hair strands and masses, clean detailed fabric/materials, soft cinematic light, crisp silhouette, coherent hands and limbs, full body visible, comfortable explorer clothing and a small backpack, simple bright background. Keep facial identity stronger than stylization. No text, logos, labels or extra people.`;
const REFINEMENT_MASTER=`Internally re-check against the ORIGINAL reference before finalizing: increase facial resemblance; refine facial features and proportions more precisely; correct drift in face shape, eyes, eyebrows, nose, mouth, cheeks, hairline, age impression and head-to-body proportion. Apply the effect of repeated feedback: make the face resemble the original more; refine face similarity; match the original facial features and proportions more precisely. Identity wins over style.`;
const DIRECTIONS=[{key:'LIVELY',label:'활발한 탐험가',subtitle:'호기심 가득, 새로운 길을 발견하는 느낌',tags:['발랄해요','활동적이에요','도전해요'],prompt:'bright energetic smile, open welcoming gesture or light forward-moving pose, adventurous and curious energy'},{key:'WARM',label:'따뜻한 탐험가',subtitle:'친근하고 포근하게 함께하는 느낌',tags:['사랑스러워요','친근해요','배려해요'],prompt:'gentle warm smile, friendly relaxed pose, soft approachable expression, caring companion energy'},{key:'CALM',label:'차분한 탐험가',subtitle:'관찰하고 생각하며 꼼꼼하게 탐험하는 느낌',tags:['차분해요','집중해요','꼼꼼해요'],prompt:'calm confident smile, thoughtful composed pose, observant and prepared explorer energy'}];
const json=(status,body)=>new Response(JSON.stringify(body),{status,headers:{'content-type':'application/json; charset=utf-8','cache-control':'no-store'}});
const trace=(requestId,stage,extra={})=>console.log(JSON.stringify({component:'character-candidates',requestId,stage,...extra}));
function parseDataUrl(v=''){const m=String(v).match(/^data:(image\/(?:png|jpeg|webp));base64,(.+)$/);if(!m)throw new Error('SOURCE_PHOTO_FORMAT');const bytes=Uint8Array.from(atob(m[2]),c=>c.charCodeAt(0));if(bytes.byteLength>MAX_SOURCE_BYTES)throw new Error('SOURCE_PHOTO_TOO_LARGE');return{type:m[1],bytes}}
function directionFor(key){return DIRECTIONS.find(x=>x.key===key)||DIRECTIONS[1]}
function promptFor(direction,mode='CANDIDATE'){const master=mode==='MASTER'?`This is the FINAL MASTER refinement pass. Preserve the selected ${direction.label} mood, but regenerate from the ORIGINAL PHOTO as identity authority. Do not inherit facial drift, artifacts, or lower quality from candidate images. Raise polish and identity precision to Character Visual ID Master quality.`:`Mood direction only; mood must never alter identity: ${direction.prompt}. All candidates must have equal quality.`;return `${IDENTITY_MASTER}\n${QUALITY_MASTER}\n${master}\n${REFINEMENT_MASTER}`}
async function imageEdit(source,direction,quality,mode,requestId){trace(requestId,'provider-start',{mode,direction:direction.key});const form=new FormData();form.append('model',MODEL);form.append('prompt',promptFor(direction,mode));form.append('n','1');form.append('size','1024x1536');form.append('quality',quality);form.append('output_format','webp');form.append('image',new Blob([source.bytes],{type:source.type}),source.type==='image/png'?'reference.png':'reference.jpg');const r=await fetch('https://api.openai.com/v1/images/edits',{method:'POST',headers:{Authorization:`Bearer ${process.env.OPENAI_API_KEY}`},body:form});const data=await r.json();if(!r.ok){trace(requestId,'provider-error',{mode,direction:direction.key,status:r.status});throw Object.assign(new Error(data?.error?.message||'Image generation failed'),{status:r.status})}const x=data?.data?.[0],assetRef=x?.b64_json?`data:image/webp;base64,${x.b64_json}`:x?.url||'';if(!assetRef)throw new Error('IMAGE_ASSET_MISSING');trace(requestId,'provider-complete',{mode,direction:direction.key});return assetRef}
export default async(req)=>{const requestId=req.headers.get('x-ready-request-id')||crypto.randomUUID();trace(requestId,'function-enter',{method:req.method,paidEnabled:PAID_GENERATION_ENABLED});if(req.method!=='POST')return json(405,{ok:false,requestId,error:'METHOD_NOT_ALLOWED'});if(!PAID_GENERATION_ENABLED){trace(requestId,'blocked',{reason:'PAID_GENERATION_DISABLED'});return json(423,{ok:false,requestId,code:'PAID_GENERATION_DISABLED',error:'품질 확인 전이라 지금은 캐릭터 생성을 잠시 잠가두었어요.',message:'잠금 해제 전에는 이미지 생성 API 비용이 발생하지 않아요.'})}if(!process.env.OPENAI_API_KEY){trace(requestId,'blocked',{reason:'IMAGE_PROVIDER_NOT_CONFIGURED'});return json(503,{ok:false,requestId,error:'IMAGE_PROVIDER_NOT_CONFIGURED'})}let body;try{body=await req.json()}catch{trace(requestId,'blocked',{reason:'INVALID_JSON'});return json(400,{ok:false,requestId,error:'INVALID_JSON'})}if(body?.confirmCost!==true){trace(requestId,'blocked',{reason:'COST_CONFIRMATION_REQUIRED'});return json(409,{ok:false,requestId,error:'COST_CONFIRMATION_REQUIRED'})}let source;try{source=parseDataUrl(body.sourcePhoto)}catch(e){trace(requestId,'blocked',{reason:e.message});return json(400,{ok:false,requestId,error:e.message})}const generationId=crypto.randomUUID();try{if(body?.mode==='MASTER_REFINEMENT'){const direction=directionFor(body.direction);trace(requestId,'master-start',{direction:direction.key});const assetRef=await imageEdit(source,direction,'high','MASTER',requestId);trace(requestId,'master-complete',{direction:direction.key});return json(200,{ok:true,requestId,generationId,provider:'OPENAI',model:MODEL,quality:'high',refinementPolicy:'ORIGINAL_PHOTO_IDENTITY_MASTER',refined:{id:`${generationId}_master`,assetRef,generatorVersion:MODEL,personalityKey:direction.key,label:direction.label,quality:'master'}})}const intent=body?.intent==='SIMILAR'?'SIMILAR':'INITIAL';trace(requestId,'candidates-start',{count:3,intent});const candidates=await Promise.all(DIRECTIONS.map(async(d,k)=>{const extra=intent==='SIMILAR'?' Similarity retry: push resemblance higher while returning to the ORIGINAL photo; never copy prior facial drift.':'';const assetRef=await imageEdit(source,{...d,prompt:d.prompt+extra},'high','CANDIDATE',requestId);return{id:`${generationId}_${k+1}`,assetRef,generatorVersion:MODEL,personalityKey:d.key,label:d.label,subtitle:d.subtitle,tags:d.tags,quality:'high',identityPolicy:'ORIGINAL_PHOTO_AUTHORITY_REFINED'}}));trace(requestId,'candidates-complete',{count:3});return json(200,{ok:true,requestId,generationId,provider:'OPENAI',model:MODEL,quality:'high',candidatePolicy:'SAME_IDENTITY_SAME_QUALITY_DIFFERENT_MOOD',refinementPolicy:'ORIGINAL_PHOTO_EVERY_GENERATION',candidates})}catch(e){trace(requestId,'function-error',{providerStatus:e?.status||null,errorType:e?.status?'PROVIDER_ERROR':'ADAPTER_FAILURE'});return json(e?.status||500,{ok:false,requestId,error:e?.status?'PROVIDER_ERROR':'ADAPTER_FAILURE',providerStatus:e?.status||null,message:String(e?.message||e)})}};
export const config={path:'/api/character-candidates'};
