(() => {
  'use strict';

  const RUNTIME_VERSION = '2026.09.18-exploration-journey-v1';
  const DEFAULT_HIDE_URL = 'https://dainty-froyo-a6e427.netlify.app';
  const DEFAULT_SNAP_URL = 'https://cheerful-pothos-d1c3ee.netlify.app';
  function normalizeTestTarget(raw,fallback){if(!raw)return fallback;try{const url=new URL(raw),host=url.hostname.toLowerCase(),isLocal=host==='localhost'||host==='127.0.0.1',isQuickTunnel=host.endsWith('.trycloudflare.com'),protocolOk=isLocal?['http:','https:'].includes(url.protocol):url.protocol==='https:';if(!protocolOk||(!isLocal&&!isQuickTunnel))return fallback;url.username='';url.password='';url.search='';url.hash='';return url.href.replace(/\/$/,'')}catch{return fallback}}
  const BOOT_PARAMS=new URLSearchParams(location.search),HIDE_URL=normalizeTestTarget(BOOT_PARAMS.get('hide_target'),DEFAULT_HIDE_URL),SNAP_URL=normalizeTestTarget(BOOT_PARAMS.get('snap_target'),DEFAULT_SNAP_URL),VALID_TASK_STATES=new Set(['PENDING','COMPLETED','PARTIAL','DEFERRED','WAITING_FOR_PARENT','BLOCKED']),TRUSTED_APP_ORIGINS=new Set([new URL(HIDE_URL).origin,new URL(SNAP_URL).origin]);
  const id=prefix=>`${prefix}_${Date.now()}_${Math.random().toString(36).slice(2,8)}`,iso=ms=>new Date(ms??Date.now()).toISOString(),uniq=arr=>[...new Set(arr.filter(Boolean))];
  const taskLabels=session=>uniq([...(session?.selected||[]),...(session?.tasks||[])]);
  function suggestedApp(label=''){const x=String(label).toLowerCase();if(/단어|vocab|word|철자|뜻/.test(x))return'hide-seek';if(/라이팅|글쓰기|문장|말하기|녹음|표현|writing|speaking|sentence/.test(x))return'snap-pop';return'ready-set'}
  function emit(type,payload={}){const c=state.activeSession?.rev07;if(!c)return null;const event={event_id:id('ready_event'),type,app:'ready-set',at:iso(),session_id:c.session_id,goal_id:c.goal_id,task_id:c.active_task_id||null,lap_id:c.active_lap_id||null,payload};c.events=[...(c.events||[]),event].slice(-200);save();return event}
  function ensureContract(session=state.activeSession){if(!session)return null;if(!session.rev07){const tasks=taskLabels(session).map((label,index)=>({task_id:`task_${session.id||Date.now()}_${index+1}`,label,state:'PENDING',suggested_app:suggestedApp(label),laps:[]}));session.rev07={contract_version:'REV_07',session_id:session.id||id('session'),goal_id:id('goal'),session_state:'ACTIVE',active_task_id:tasks[0]?.task_id||null,active_lap_id:null,active_app:'ready-set',tasks,events:[],created_at:iso(session.startAt||Date.now())};if(tasks[0])startLap(tasks[0],'SESSION_START',session);save()}return session.rev07}
  const currentTask=(contract=ensureContract())=>contract?.tasks?.find(t=>t.task_id===contract.active_task_id)||null;
  function currentLap(contract=ensureContract()){const task=currentTask(contract);return task?.laps?.find(l=>l.lap_id===contract.active_lap_id)||null}
  function startLap(task,reason='TASK_START',session=state.activeSession){const c=session?.rev07;if(!task||!session||!c)return null;const now=Date.now(),lap={lap_id:id('lap'),task_id:task.task_id,started_at:iso(now),started_ms:now,ended_at:null,elapsed_ms:null,end_reason:null,result_state:null};task.laps=[...(task.laps||[]),lap];c.active_task_id=task.task_id;c.active_lap_id=lap.lap_id;c.active_app='ready-set';emit('LAP_STARTED',{reason,label:task.label});return lap}
  function endActiveLap(reason='TASK_CHANGE',resultState=null){const c=ensureContract(),lap=currentLap(c),task=currentTask(c);if(!lap||lap.ended_at)return lap;const now=Date.now();lap.ended_at=iso(now);lap.elapsed_ms=Math.max(0,now-Number(lap.started_ms||now));lap.end_reason=reason;lap.result_state=resultState||task?.state||'PENDING';c.active_lap_id=null;emit('LAP_ENDED',{reason,result_state:lap.result_state,elapsed_ms:lap.elapsed_ms});return lap}
  function plannerBindingForCanonical(taskId){const host=state.activeSession;return host?.homeworkTaskMap?.find(entry=>entry.canonical_task_id===taskId)||null}
  function publishTaskResult(taskId,nextState,source='READY_UI'){const binding=plannerBindingForCanonical(taskId),host=state.activeSession;if(!binding?.planner_id||!window.ReadyBaseRuntimeV1?.publishPlannerResult)return null;const tm=window.ReadyBaseRuntimeV1?.sessionTimes?.()||{focus:0,paused:0};return window.ReadyBaseRuntimeV1.publishPlannerResult(binding.planner_id,nextState,{focusMs:tm.focus,pausedMs:tm.paused,source,sessionId:host?.id||null})}
  function setTaskState(taskId,nextState,source='READY_UI'){const c=ensureContract(),task=c?.tasks?.find(t=>t.task_id===taskId);if(!task||!VALID_TASK_STATES.has(nextState))return false;const previous=task.state;task.state=nextState;task.updated_at=iso();emit('TASK_STATE_CHANGED',{task_id:taskId,previous,next:nextState,source,provenance:'SESSION_DERIVED'});if(nextState!=='PENDING')publishTaskResult(taskId,nextState,source);save();renderContractUI();return true}
  function switchTask(taskId){const c=ensureContract(),next=c?.tasks?.find(t=>t.task_id===taskId);if(!next||next.task_id===c.active_task_id)return;endActiveLap('TASK_CHANGE',currentTask(c)?.state||'PENDING');c.active_task_id=next.task_id;startLap(next,'NEXT_TASK',state.activeSession);save();renderContractUI()}
  const appUrl=app=>app==='hide-seek'?HIDE_URL:app==='snap-pop'?SNAP_URL:location.href;
  function readyReturnUrl(){const url=new URL(`${location.origin}${location.pathname}`);if(HIDE_URL!==DEFAULT_HIDE_URL)url.searchParams.set('hide_target',HIDE_URL);if(SNAP_URL!==DEFAULT_SNAP_URL)url.searchParams.set('snap_target',SNAP_URL);return url.href}
  function launchSpecialist(app){const session=state.activeSession,c=ensureContract(session),task=currentTask(c),lap=currentLap(c)||(task?startLap(task,'SPECIALIST_ROUTE',session):null);if(!session||!c||!task||!lap||!['hide-seek','snap-pop'].includes(app))return;c.active_app=app;emit('APP_SWITCH',{from:'ready-set',to:app,lap_ended:false});save();const url=new URL(appUrl(app));url.searchParams.set('session_id',c.session_id);url.searchParams.set('goal_id',c.goal_id);url.searchParams.set('task_id',task.task_id);url.searchParams.set('lap_id',lap.lap_id);url.searchParams.set('return_target',readyReturnUrl());url.searchParams.set('snap_target',SNAP_URL);url.searchParams.set('target_time_ms',String(session.targetMs||0));url.searchParams.set('session_start_at',String(session.startAt||0));url.searchParams.set('issue_ms',String(session.issueMs||session.pausedMs||0));if(session.pausedAt)url.searchParams.set('paused_at',String(session.pausedAt));url.searchParams.set('from_app','ready-set');location.assign(url.href)}
  function normalizeInboundState(raw){if(!raw)return null;if(VALID_TASK_STATES.has(raw))return raw;if(raw==='HELP_NEEDED')return'BLOCKED';return null}
  function applyInboundResult({session_id,goal_id,task_id,lap_id,task_state,from_app,event_id=null}){const c=ensureContract();if(!c||!session_id||session_id!==c.session_id)return false;if(!goal_id||goal_id!==c.goal_id)return false;if(event_id&&c.applied_event_ids?.includes(event_id))return false;const task=c.tasks.find(t=>t.task_id===task_id);if(!task)return false;const lap=lap_id?(task.laps||[]).find(l=>l.lap_id===lap_id):null;if(!lap||lap.ended_at)return false;if(c.active_lap_id&&c.active_lap_id!==lap.lap_id)return false;const normalized=normalizeInboundState(task_state);if(!normalized)return false;c.active_app='ready-set';c.active_task_id=task.task_id;c.active_lap_id=lap.lap_id;setTaskState(task.task_id,normalized,from_app||'SPECIALIST');if(['COMPLETED','BLOCKED'].includes(normalized))endActiveLap('SPECIALIST_RESULT',normalized);if(event_id)c.applied_event_ids=[...(c.applied_event_ids||[]),event_id].slice(-200);emit('APP_RETURN',{from:from_app||'specialist',task_state:normalized});save();renderContractUI();return true}
  function consumeReturnQuery(){const p=new URLSearchParams(location.search),args={session_id:p.get('session_id'),goal_id:p.get('goal_id'),task_id:p.get('task_id'),lap_id:p.get('lap_id'),task_state:p.get('task_state'),from_app:p.get('from_app'),event_id:p.get('event_id')};if(!args.session_id||!args.task_id||!applyInboundResult(args))return;['session_id','goal_id','task_id','lap_id','task_state','from_app','event_id'].forEach(k=>p.delete(k));const clean=`${location.pathname}${p.toString()?`?${p}`:''}${location.hash}`;history.replaceState(null,'',clean)}
  function handleLearningEvent(messageEvent){if(!TRUSTED_APP_ORIGINS.has(messageEvent.origin))return;const e=messageEvent.data?.type==='TAKY_LEARNING_EVENT'?messageEvent.data.event:null;if(!e)return;const taskState=e.type==='TASK_COMPLETED'?'COMPLETED':e.type==='TASK_BLOCKED'||e.type==='HELP_NEEDED'?'BLOCKED':e.type==='TASK_PARTIAL'?'PARTIAL':null;if(!taskState)return;applyInboundResult({session_id:e.session_id,goal_id:e.goal_id,task_id:e.task_id,lap_id:e.lap_id,task_state:taskState,from_app:e.app,event_id:e.event_id})}
  const labelState=value=>({PENDING:'미확정',COMPLETED:'완료',PARTIAL:'일부 남음',DEFERRED:'다음에',WAITING_FOR_PARENT:'부모 도움',BLOCKED:'막힘'})[value]||value;
  function injectStyles(){if(document.getElementById('readyRev07Style'))return;const style=document.createElement('style');style.id='readyRev07Style';style.textContent=`.rev07-panel{margin:12px 0 4px;padding:14px;border-radius:22px;background:rgba(255,255,255,.72);border:1px solid rgba(40,30,25,.10)}.rev07-panel small{display:block;opacity:.62;font-weight:800}.rev07-panel h3{margin:4px 0 10px;font-size:17px}.rev07-row{display:flex;gap:8px;flex-wrap:wrap}.rev07-row button{border:0;border-radius:999px;padding:9px 12px;font-weight:800;background:#fff}.rev07-row button.primary{background:#2a231f;color:#fff}.rev07-tasks{margin-top:10px;display:grid;gap:6px}.rev07-task{display:flex;justify-content:space-between;padding:8px 10px;border:0;border-radius:14px;background:rgba(255,255,255,.62)}.rev07-task.active{outline:2px solid rgba(42,35,31,.3)}.rev07-modal{position:fixed;inset:0;z-index:9999;display:flex;align-items:flex-end;justify-content:center;background:rgba(20,16,14,.45)}.rev07-modal[hidden]{display:none}.rev07-sheet{width:min(680px,100%);max-height:82vh;overflow:auto;background:#fffaf5;border-radius:30px 30px 0 0;padding:20px 18px calc(20px + env(safe-area-inset-bottom))}.rev07-wrap-task{padding:12px 0;border-top:1px solid rgba(0,0,0,.08)}.rev07-state-grid{display:flex;gap:6px;flex-wrap:wrap}.rev07-state-grid button{border:1px solid rgba(0,0,0,.12);background:#fff;border-radius:999px;padding:8px 10px}.rev07-state-grid button.on{background:#2a231f;color:#fff}.rev07-wrap-actions{display:flex;gap:8px;padding-top:14px}.rev07-wrap-actions button{flex:1;border:0;border-radius:16px;padding:13px;font-weight:900}.rev07-wrap-actions .end{background:#2a231f;color:#fff}.rev07-guide{padding:10px 12px;border-radius:16px;background:#f3eadf;margin-bottom:12px}`;document.head.appendChild(style)}
  function ensurePanel(){const control=document.querySelector('#focusView .controlPanel');if(!control)return null;let panel=document.getElementById('readyRev07Panel');if(!panel){panel=document.createElement('section');panel.id='readyRev07Panel';panel.className='rev07-panel';control.before(panel);panel.addEventListener('click',event=>{const app=event.target.closest('[data-rev07-app]')?.dataset.rev07App;if(app)return launchSpecialist(app);const taskId=event.target.closest('[data-rev07-task]')?.dataset.rev07Task;if(taskId)return switchTask(taskId)})}return panel}
  function renderContractUI(){const c=ensureContract(),panel=ensurePanel();if(!c||!panel)return;const task=currentTask(c);panel.innerHTML=`<small>ONE SESSION · CONTINUOUS TIMER</small><h3>${task?escapeHtml(task.label):'현재 과제 없음'} · ${task?labelState(task.state):''}</h3><div class="rev07-row"><button class="primary" data-rev07-app="hide-seek">Hide & Seek</button><button class="primary" data-rev07-app="snap-pop">Snap & Pop</button></div><div class="rev07-tasks">${c.tasks.map(t=>`<button class="rev07-task ${t.task_id===c.active_task_id?'active':''}" data-rev07-task="${t.task_id}"><span>${escapeHtml(t.label)}</span><strong>${labelState(t.state)}</strong></button>`).join('')}</div>`;const mission=document.getElementById('focusMission');if(mission&&task)mission.textContent=task.label}
  function ensureWrapUp(){let modal=document.getElementById('readyRev07Wrap');if(modal)return modal;modal=document.createElement('div');modal.id='readyRev07Wrap';modal.className='rev07-modal';modal.hidden=true;modal.innerHTML=`<div class="rev07-sheet"><h2>오늘은 여기까지?</h2><div class="rev07-guide" id="rev07GuideLine"></div><div id="rev07WrapTasks"></div><div class="rev07-wrap-actions"><button id="rev07CancelEnd">계속하기</button><button class="end" id="rev07ConfirmEnd">세션 종료</button></div></div>`;document.body.appendChild(modal);modal.addEventListener('click',event=>{const btn=event.target.closest('[data-wrap-state]');if(!btn)return;setTaskState(btn.dataset.taskId,btn.dataset.wrapState,'WRAP_UP');renderWrapUp()});document.getElementById('rev07CancelEnd').onclick=()=>{modal.hidden=true};document.getElementById('rev07ConfirmEnd').onclick=finalizeSession;return modal}
  function renderWrapUp(){const c=ensureContract(),modal=ensureWrapUp();if(!c)return;const unresolved=c.tasks.filter(t=>t.state==='PENDING');document.getElementById('rev07GuideLine').textContent=unresolved.length?`${state.guide?.name||'길잡이'}: ${unresolved.map(t=>t.label).join(', ')} 상태만 짧게 알려줘.`:`${state.guide?.name||'길잡이'}: 좋아. 빠진 상태 없이 정리됐어.`;document.getElementById('rev07WrapTasks').innerHTML=c.tasks.map(t=>`<div class="rev07-wrap-task"><b>${escapeHtml(t.label)} · ${labelState(t.state)}</b><div class="rev07-state-grid">${['COMPLETED','PARTIAL','DEFERRED','WAITING_FOR_PARENT','BLOCKED'].map(s=>`<button class="${t.state===s?'on':''}" data-wrap-state="${s}" data-task-id="${t.task_id}">${labelState(s)}</button>`).join('')}</div></div>`).join('');document.getElementById('rev07ConfirmEnd').disabled=unresolved.length>0;modal.hidden=false}
  function openWrapUp(){ensureContract();renderWrapUp()}
  function finalizeSession(){const c=ensureContract();if(!c)return;if(c.tasks.some(t=>t.state==='PENDING')){toast('미확정 과제 상태를 먼저 정리해 주세요.');return renderWrapUp()}endActiveLap('SESSION_END',currentTask(c)?.state||'PENDING');c.session_state='ENDED';c.ended_at=iso();c.active_app='ready-set';emit('SESSION_ENDED',{task_states:c.tasks.map(t=>({task_id:t.task_id,state:t.state}))});save();document.getElementById('readyRev07Wrap').hidden=true;originalCompleteSession()}
  function validateContract(){const c=state.activeSession?.rev07;if(!c)return{ok:false,reason:'NO_ACTIVE_REV07_SESSION'};const activeLaps=c.tasks.flatMap(t=>t.laps||[]).filter(l=>!l.ended_at),taskIds=new Set(c.tasks.map(t=>t.task_id)),checks={oneSessionId:!!c.session_id,oneGoalId:!!c.goal_id,oneActiveTask:!c.active_task_id||taskIds.has(c.active_task_id),atMostOneActiveLap:activeLaps.length<=1,activeLapPointerValid:!c.active_lap_id||activeLaps.some(l=>l.lap_id===c.active_lap_id),validTaskStates:c.tasks.every(t=>VALID_TASK_STATES.has(t.state)),singleActiveApp:['ready-set','hide-seek','snap-pop'].includes(c.active_app),plannerResultBridge:typeof window.ReadyBaseRuntimeV1?.publishPlannerResult==='function'};return{ok:Object.values(checks).every(Boolean),checks}}
  const originalCompleteSession=completeSession,originalStart=document.getElementById('startBtn')?.onclick,originalNav=nav;
  function patchHandlers(){const start=document.getElementById('startBtn');if(start&&originalStart)start.onclick=async function(event){await originalStart.call(this,event);if(state.activeSession){ensureContract();renderContractUI()}};const end=document.getElementById('completeBtn');if(end){end.textContent='세션 종료';end.onclick=openWrapUp}nav=function(name){originalNav(name);if(name==='focus'&&state.activeSession)setTimeout(renderContractUI,0)}}
  function boot(){document.documentElement.dataset.readyRuntime=RUNTIME_VERSION;injectStyles();patchHandlers();ensureWrapUp();if(state.activeSession){ensureContract();consumeReturnQuery();renderContractUI()}window.addEventListener('message',handleLearningEvent);window.ReadySetRev07=Object.freeze({version:RUNTIME_VERSION,contract:()=>state.activeSession?.rev07?structuredClone(state.activeSession.rev07):null,validate:validateContract,launchSpecialist,setTaskState,switchTask,openWrapUp,routing:()=>({hide_url:HIDE_URL,snap_url:SNAP_URL,test_mode:HIDE_URL!==DEFAULT_HIDE_URL||SNAP_URL!==DEFAULT_SNAP_URL})})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();