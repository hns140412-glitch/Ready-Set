(() => {
'use strict';
const F=window.ReadyFoundationV1,KEY='ready_foundation_v1';
const today=()=>new Date().toLocaleDateString('sv-SE');
const datePlus=(base,n)=>{const d=new Date(`${base}T12:00:00`);d.setDate(d.getDate()+n);return d.toLocaleDateString('sv-SE')};
const validDate=v=>/^\d{4}-\d{2}-\d{2}$/.test(String(v||''));
const empty=()=>({mode:'STANDALONE',profiles:[],overrides:[],sourceStatus:{},assignments:[],units:[],batches:[],conditionEvidence:{},plans:[]});
const load=()=>{const raw=localStorage.getItem(KEY);return raw?JSON.parse(raw):empty()};
const save=s=>{localStorage.setItem(KEY,JSON.stringify(s));window.dispatchEvent(new Event('ready-foundation-change'));return s};
const activeRole=()=>window.ReadyRoleContextV1?.current?.()||(document.documentElement.dataset.readyRole==='PARENT'?'parent':new URLSearchParams(location.search).get('role'));
const parent=()=>{if(activeRole()!=='parent')throw Error('PARENT_ROLE_REQUIRED')};
const cycleBoundarySources=new Set(['TALENT_BOOK_ASSIGNMENT','ENGLISH_ACADEMY_PACKAGE','SCIENCE_ACADEMY_HOMEWORK']);
const planningDeadline=f=>{const d=f.deadlineBoundary||f.deadline||null;if(!validDate(d))return null;return cycleBoundarySources.has(f.sourceType)?datePlus(d,-1):d};
const plannerStore=()=>{try{return JSON.parse(localStorage.getItem('readyset_planner_v1')||'{}')}catch{return {}}};
const assignmentFacts=()=>Object.values(plannerStore().assignmentFacts||{}).filter(f=>f.confirmationState==='FACT_CONFIRMED'&&f.lifecycle!=='ARCHIVED').map(f=>({id:f.assignmentId,title:f.title||(f.talentSubject?`재능 · ${f.talentSubject}`:f.subject)||'숙제',subject:f.subject||f.talentSubject||'숙제',volume:f.range||'',deadline:planningDeadline(f),source:'LEGACY_CONFIRMED_FACT',sourceType:f.sourceType||null,boundary:f.deadlineBoundary||null}));
const TALENT_LOAD={'연산':[1,1],'한자':[1,1],'국어':[2,2],'사회':[2,2],'수학':[3,3],'생각하는 피자':[3,3]};
const ENGLISH_LOAD={WORKBOOK_RANGE:[2,2],PRINT:[2,2],vocabulary:[1,1],listening:[1,1],recording:[2,2],writing:[3,3],other:[2,2]};
function legacyConfirmedFacts(){return Object.values(plannerStore().assignmentFacts||{}).filter(f=>f.confirmationState==='FACT_CONFIRMED'&&f.lifecycle!=='ARCHIVED')}
function makeUnit(id,assignmentId,label,loads=[2,2],kind='GENERAL'){return{id,assignment_id:assignmentId,label:String(label||'숙제 단위'),authority:'LEARNING/SUBJECT',cognitiveLoad:Number(loads[0]),activityLoad:Number(loads[1]),kind}}
function planningUnits(){const out=[];for(const f of legacyConfirmedFacts()){const deadline=planningDeadline(f);if(!deadline||deadline<today())continue;if(f.sourceType==='TALENT_BOOK_ASSIGNMENT'){if(String(f.range||'').trim())out.push(makeUnit(`foundation-unit:${f.assignmentId}:range`,f.assignmentId,f.range,TALENT_LOAD[f.talentSubject]||[2,2],'TALENT_BOOK_RANGE'));continue}if(f.sourceType==='ENGLISH_ACADEMY_PACKAGE'){if(String(f.range||'').trim())out.push(makeUnit(`foundation-unit:${f.assignmentId}:workbook`,f.assignmentId,f.range,ENGLISH_LOAD.WORKBOOK_RANGE,'WORKBOOK_RANGE'));for(const [k,v] of Object.entries(f.printUnits||{}))if(String(v||'').trim())out.push(makeUnit(`foundation-unit:${f.assignmentId}:print:${k}`,f.assignmentId,`${k} 프린트 · ${v}`,ENGLISH_LOAD.PRINT,'PRINT'));for(const [k,v] of Object.entries(f.components||{}))if(String(v||'').trim())out.push(makeUnit(`foundation-unit:${f.assignmentId}:component:${k}`,f.assignmentId,`${k} · ${v}`,ENGLISH_LOAD[k]||ENGLISH_LOAD.other,k));continue}const label=String(f.range||f.title||f.subject||'').trim();if(label)out.push(makeUnit(`foundation-unit:${f.assignmentId}:main`,f.assignmentId,label,[2,2],f.sourceType||'GENERAL'))}const s=load();for(const a of s.assignments||[]){if(!a?.id||!validDate(a.deadline)||a.deadline<today())continue;if(out.some(u=>u.assignment_id===a.id))continue;out.push(makeUnit(`foundation-unit:${a.id}:manual`,a.id,a.volume||a.title||'숙제',[2,2],'MANUAL_CONFIRMED'))}return out}
function planningDates(assignments){const ds=assignments.map(a=>a.deadline).filter(d=>validDate(d)&&d>=today()).sort();if(!ds.length)return[];const max=ds.at(-1),out=[];for(let d=today(),i=0;d<=max&&i<62;d=datePlus(d,1),i++)out.push(d);return out}
function actualHistory(units=[]){const p=plannerStore(),byAssignment=new Map();for(const u of units){if(!byAssignment.has(u.assignment_id))byAssignment.set(u.assignment_id,[]);byAssignment.get(u.assignment_id).push(u)}const rows=[];for(const [localDate,day] of Object.entries(p.days||{}))for(const task of day.tasks||[]){if(!['COMPLETED','IN_PROGRESS','WAITING_FOR_PARENT'].includes(task.status)&&!(task.learningReports||[]).length)continue;const direct=task.sourceUnitId&&units.find(u=>u.id===task.sourceUnitId),matches=direct?[direct]:(byAssignment.get(task.sourceAssignmentId)||[]);for(const unit of matches)rows.push({unit_id:unit.id,status:task.status,localDate,taskId:task.id,selected:task.selected===true,learningReports:task.learningReports||[]})}return rows}
const service={load,assignmentFacts,
 importBaseline(authority){const s=load();if(s.baselineImported)return;s.baselineImported=true;if(!s.profiles.length){s.mode='HYBRID';s.profiles.push(F.profile({id:'legacy-confirmed-baseline',revision:1,state:'ACTIVE',effective_from:authority.capturedAt.slice(0,10),effective_to:null,provenance:{source:'NOTION_SNAPSHOT',capturedAt:authority.capturedAt},events:authority.rows.map((r,i)=>F.normalize('NOTION',{id:'baseline:'+i,key:r.activity+':'+r.weekday,title:r.activity,weekday:['일','월','화','수','목','금','토'].indexOf(r.weekday),start:r.start,end:r.end,parent_editable:true,planner_movable:false}))}))}save(s)},
 saveProfile(input){parent();const s=load(),p=F.profile(input);if(s.profiles.some(x=>x.id===p.id)){s.profiles=F.revise(s.profiles,p.id,p.effective_from,p)}else s.profiles.push(p);save(s);if(s.lastPlannerInput)service.prepare({...s.lastPlannerInput,from:today(),actualHistory:actualHistory(s.lastPlannerInput.units||[])});return s},
 saveOverride(o){parent();const s=save(F.override(load(),o));if(s.lastPlannerInput)service.prepare({...s.lastPlannerInput,from:today(),actualHistory:actualHistory(s.lastPlannerInput.units||[])});return s},
 support(evidence){parent();const s=load();s.conditionEvidence[today()]={requestedRest:evidence.requestedRest===true};save(s);if(s.lastPlannerInput)service.prepare({...s.lastPlannerInput,from:today(),actualHistory:actualHistory(s.lastPlannerInput.units||[])});return s},
 capture(action,payload){parent();const s=load();let b=s.batches.at(-1);if(!b||b.state==='COMMITTED'){b={id:'batch:'+Date.now(),state:'CAPTURING',items:[]};s.batches.push(b)}b=F.capture(b,action,payload);s.batches[s.batches.length-1]=b;if(b.state==='COMMITTED')s.assignments.push(...b.facts);save(s);if(b.state==='COMMITTED')setTimeout(()=>service.prepareConfirmedFacts(),0);return b},
 prepare(input){const s=load();if(input.from<today())throw Error('PAST_PLAN_WRITE_FORBIDDEN');const history=Array.isArray(input.actualHistory)?input.actualHistory:actualHistory(input.units||[]);const result=F.plan({...input,actualHistory:history,state:s,assignments:[...assignmentFacts(),...s.assignments],conditionEvidence:s.conditionEvidence});s.lastPlannerInput=JSON.parse(JSON.stringify({...input,actualHistory:history}));s.plans.push({createdAt:new Date().toISOString(),...result});save(s);const p=plannerStore();p.days=p.days||{};const protectedIds=new Set();for(const [d,day] of Object.entries(p.days)){for(const t of day.tasks||[])if(d<input.from||t.status!=='PLANNED'||t.selected||t.learningReports?.length)protectedIds.add(t.id)}for(const [d,day] of Object.entries(p.days))if(d>=input.from)day.tasks=(day.tasks||[]).filter(t=>t.authority!=='PLANNER/MAIN'||t.projection!=='TODAY_TASK'||protectedIds.has(t.id));for(const t of result.candidates){if(protectedIds.has(t.id))continue;const day=p.days[t.localDate]||(p.days[t.localDate]={localDate:t.localDate,tasks:[]});day.tasks.push(t)}localStorage.setItem('readyset_planner_v1',JSON.stringify(p));window.ReadyHomeHomeworkUIV1?.render?.();window.ReadyStageD?.renderPlanner?.();window.ReadyStageF?.render?.();return result},
 todayTasks(){return load().plans.at(-1)?.candidates.filter(t=>t.localDate===today())||[]},actualHistory
};
service.prepareConfirmedFacts=()=>{const assignments=[...assignmentFacts(),...(load().assignments||[])].filter(a=>validDate(a.deadline)&&a.deadline>=today()),units=planningUnits(),dates=planningDates(assignments);if(!assignments.length||!units.length||!dates.length)return{ok:false,reason:'NO_CONFIRMED_FACT_UNITS',candidates:[],unresolved:[]};const history=actualHistory(units),result=service.prepare({from:today(),dates,units,carryOver:[],actualHistory:history});return{...result,ok:result.candidates.length>0,reason:result.candidates.length?null:(result.unresolved[0]?.reason||'NO_PLANNER_CANDIDATE'),assignmentCount:assignments.length,unitCount:units.length,actualHistoryCount:history.length}};
function allocationSummary(plan,legacy){if(plan?.candidates?.length)return{ok:true,authority:'FOUNDATION_PLANNER',count:plan.candidates.length,unresolved:plan.unresolved||[]};return{ok:false,authority:'FOUNDATION_PLANNER',reason:plan?.reason||plan?.unresolved?.[0]?.reason||legacy?.reason||'NO_PLANNER_CANDIDATE',unresolved:plan?.unresolved||[]}}
function installAssignmentBridge(){const A=window.ReadyAssignmentModel;if(!A||A.__foundationPlannerBridge)return false;const original={upsertTalentPackage:A.upsertTalentPackage?.bind(A),upsertEnglishPackage:A.upsertEnglishPackage?.bind(A),confirmFact:A.confirmFact?.bind(A)};if(!original.upsertTalentPackage||!original.upsertEnglishPackage||!original.confirmFact)return false;const W=Object.freeze({...A,__foundationPlannerBridge:true,upsertTalentPackage(args={}){const r=original.upsertTalentPackage(args),plan=service.prepareConfirmedFacts();return{...r,legacyAllocation:r?.allocation||null,foundationPlan:plan,allocation:allocationSummary(plan,r?.allocation)}},upsertEnglishPackage(args={}){const r=original.upsertEnglishPackage(args),plan=service.prepareConfirmedFacts();return{...r,legacyAllocation:r?.allocation||null,foundationPlan:plan,allocation:allocationSummary(plan,r?.allocation)}},confirmFact(id,actor='PARENT'){const r=original.confirmFact(id,actor),plan=service.prepareConfirmedFacts();return r&&typeof r==='object'?{...r,foundationPlan:plan}:r}});window.ReadyAssignmentModel=W;return true}
function bridgeBoot(attempt=0){if(installAssignmentBridge())return;if(attempt<120)setTimeout(()=>bridgeBoot(attempt+1),25)}
window.ReadyFoundationControlV1=Object.freeze(service);bridgeBoot();
})();